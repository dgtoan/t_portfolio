part of 'contact_me.dart';

class ContactMeItem extends StatelessWidget {
  const ContactMeItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 100,
      height: 40,
      color: Colors.blue,
    );
  }
}