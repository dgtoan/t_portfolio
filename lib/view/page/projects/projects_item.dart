part of 'projects.dart';

class ProjectsItem extends StatelessWidget {
  const ProjectsItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 100,
      height: 100,
      color: Colors.yellow,
    );
  }
}