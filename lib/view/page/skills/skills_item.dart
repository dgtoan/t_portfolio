part of 'skills.dart';

class SkillsItem extends StatelessWidget {
  const SkillsItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 200,
      height: 200,
      color: Colors.blue,
    );
  }
}